package app.aaps.plugins.main.general.dashboard.compose

/**
 * Layout density for the dashboard hero + surrounding chrome.
 *
 * [SimpleOneScreen] optimizes for a single viewport without scrolling (compact hero, pills, tight spacing).
 */
enum class DashboardHeroLayoutProfile {
    Default,
    SimpleOneScreen,
}
