# Merge log: `upstream/dev` + `mtr/dev_OAPSAIMI` → `dev_OAPSAIMI_RB` (2026-09-24)

Two merges on the same day, in order.

## 1. `upstream/dev` → `dev_OAPSAIMI_RB`

- **Source**: `upstream/dev` @ `7e1d537d49` (74 commits since merge-base `73ce4b30d7`)
- **Target pre-merge tag**: `premerge-dev-2026-09-24` = `29886e5439`
- **Merge commit**: `58c64b7675`
- **Size**: 229 files changed (7348 insertions, 2450 deletions)
- **Conflicts**: 15 content conflicts, all resolved by hand

### What arrived

- Plugin enable/visibility flags become declared **enforcements**
  (`Enforcement` / `enforce()` / `enforceEnabledOnlyWhen()` /
  `PluginBase.showInList()` with no `PluginType`). `alwaysEnabled`,
  `neverVisible`, `enableByDefault`, `specialEnableCondition` and
  `specialShowInListCondition` are gone. `showInList()` also hides a plugin
  this build forces off.
- **SafetyPlugin** alone owns the temp-basal rule for APS plugins.
- **LoopPlugin** SMB fallback is an owned job cancelled in `onStop`
  (`scheduleSmbFallback`), not a bare `appScope.launch`.
- Composables use `stringResource` instead of `getString` / `rh.gs`. Icon-only
  buttons get content descriptions. Import can keep pump settings. Wear
  preferences can reload every cached flow (`Observed` + read).
- Never run the loop on a build without an APS of its own.

### Conflict resolutions (15)

| File | Decision |
|---|---|
| `PluginDescription.kt` | Combine: keep fork builders (`fragmentClass`, `alwaysVisible`, `simpleModePosition`), take upstream `enforce*`, drop dead `alwaysEnabled`/`neverVisible` |
| `PluginDescriptionTest.kt` | Take upstream whole under `androidHostTest`; delete `commonTest` copy |
| `LoopPlugin.kt` | Keep `scheduleSmbFallback`, `uiInteraction`, `CancellationException`; no bare launch |
| `OpenAPSAutoISFPlugin.kt` | Stay on `androidMain`; drop `special*` overrides |
| `SafetyPlugin.kt` / `BgQualityCheckPlugin.kt` | Keep both sides' imports |
| `LoopPluginTest.kt` | One `buildLoopPlugin()` matching fork ctor + `UiInteraction` mock |
| OpenAPSAMA/SMB tests | Import union (`Loop` + `PluginType`) |
| `PluginLifetimeWorkScanTest.kt` | Short docs; drop `LoopPlugin#executeInvokeInternal`; keep `startPeriodicLoop` + Eversense |
| `ComposeMainActivity.kt` | Keep `TextAlign`/`dp` and `glassRoutes(onShowDeliveryError, onOpenAimiContext)`. Theirs' OverviewScreen args were a mis-splice of a different call site |
| `GraphsSection.kt` | Keep fork `ModesPanel` event grid; `GraphEditButton` is gone on fork |
| `BolusCarbsScreen.kt` | Keep `looksInhaled` (Afrezza); drop unused `TextResolver` |
| wear `PreferencesImpl.kt` | Take theirs `stringFlows[key]?.flow?.value` (ours does not type-check against `Observed<T>`) |
| `.gitignore` | Keep both `__pycache__/` and `~$*` |

### Fork fallout (upstream has no AIMI/BOOST/AutoISF)

- AIMI / Boost / BoostV5 / AutoISF: drop `specialEnableCondition` /
  `specialShowInListCondition`. Temp basal stays in SafetyPlugin. AutoISF
  stays on `androidMain`.
- Dexcom ONE+ / Libre3 native: availability moves into
  `.showInList { availabilityProvider.isAvailable() }`.
- `OverviewPlugin`: `.alwaysEnabled(true)` → `.enforce(EnforcedState.Enabled)`.
- `RemoteControlPlugin`: drop `neverVisible`;
  `enforceEnabledOnlyWhen { config.AAPSCLIENT }`; keep `showInList`.
- Visibility tests: `showInList()` + `enforcedState()).isNull()`.

## 2. `mtr/dev_OAPSAIMI` → `dev_OAPSAIMI_RB`

- **Source**: `mtr/dev_OAPSAIMI` @ `166ddb6db0` (4 commits since `468cf7a034`)
- **Merge commit**: `9a5238c580`
- **Size**: 55 files (6965 insertions, 94 deletions)
- **Conflicts**: 31 (5 content, 1 delete/modify, 25 added under a renamed source set)

### What arrived

- Garmin sport mode + FCL temporary target.
- Meal boost cap evaluation + basal shadow export records.
- Auditor/RateLimit/Validator/TickState/SnapshotLevels/TickRing tests.
- AwakeRestingHeartRate calculation and tests.
- Auditor profile-factor path: `OApsAIMIAuditorProfileFactors` key,
  `AuditorProfileFactor*`, `WorkingIsf`, stress/pre floor on `OapsProfileAimi`,
  dose-side helpers in `DetermineBasalAIMI2`.

### Conflict resolutions (31)

- `OapsProfileAimi` / `OpenAPSAIMIPlugin`: keep ketoacidosis fields **and**
  take stress/pre-floor fields and assignments.
- `DetermineBasalAIMI2`: take new auditor ISF/target helpers; keep English
  KDoc on `AimiPkpdBgiDeviationAndTargetsStage`.
- `GarminPluginTest`: keep fork ctor (SP mock + `notificationManager`).
- `LoopHubTest`: drop duplicate `Preferences` import.
- `BooleanKey`: keep HEAD delete of `src/main` (file is `commonMain`); port
  `OApsAIMIAuditorProfileFactors` into `commonMain` with `KeysStrings`
  title/summary and `dependency = AimiAuditorEnabled`. Strings landed in
  `androidMain/res`.
- 25 `UA` paths: accept at flipped locations (`androidMain` / `androidHostTest`).
  No file left on a pre-flip path.

## Post-merge fallout (working tree, after both merge commits)

Files fixed so the tree builds and the new upstream key tests pass; **not yet in a commit**:

1. `core/ui/.../strings.xml`: duplicate `a11y_min_button_description` /
   `a11y_plus_button_description` (merge kept both upstream's commented
   definitions and the fork's bare NumberPicker copies). Kept the commented
   ones; deleted the bare pair.
2. `SliderWithButtons.kt`: upstream's a11y `stepText` still named
   `valueFormatRef` / `formatAsInt`; fork's `formatSliderDisplayValue` uses
   `unitFormat`. Adapted the call.
3. `implementation/.../PreferencesImpl.kt`: fork still used the old
   `getOrCreate { MutableStateFlow(value) }` after upstream changed
   `FlowCache.getOrCreate` to take `read: () -> T`. Now
   `getOrCreate(key.key) { value }.value = value`.
4. `core/keys/.../IntKey.kt` + `StringKey.kt`: removed empty-title stubs
   `GarminLocalHttpPort` / `GarminRequestKey` that shared the stored strings
   `communication_http_port` / `garmin_aaps_key` with the live
   `GarminIntKey.LocalHttpPort` / `GarminStringKey.RequestKey` (the keys
   `GarminPlugin` actually reads). Upstream has neither stub; they were fork
   duplicates. Fixes `PreferenceKeyRoundTripTest` and the snapshot duplicate
   check.
5. `RawPreferenceStoreScanTest`: skip `/docs/` (reference snippets) and
   `/tools/` (standalone samples); list the 36 fork production files that
   still reach `SP`/`SharedPreferences` under `toDo` with reasons, so the
   debt is visible and can only shrink.
6. `app/src/testFull/resources/prefs-schema.txt`: snapshot rewritten by
   `PreferenceKeySnapshotTest` (the new AIMI/mtr keys plus removal of the two
   Garmin stub lines). Read the diff and commit with the key changes.
7. `docs/MERGE_DEV_2026-09-24.md` + `docs/NON_REGRESSION_CHECKLIST.md`
   latest-merge pointer.

### Verified

- `scripts/merge_invariants.sh` vs `premerge-dev-2026-09-24`: only expected
  gains (openAPSAIMI file count, `AAPS_DIR_ACCESS_LOST`, `PLUGIN_WORK_FAILED`,
  string counts).
- `injectMetroMembers` sweep: nothing lost on either parent.
- No leftover `specialEnableCondition` / `.alwaysEnabled(` / `.neverVisible(`
  call sites (docs only).
- Eversense: plugin dir present; `SourceSensor.EVERSENSE_E3` / `EVERSENSE_365`
  present; no Eversense paths in the incoming upstream diff.
- `:app:assembleFullDebug` and `:wear:assembleFullDebug` **BUILD SUCCESSFUL**.
- `:app:testFullDebugUnitTest` and `:wear:testFullDebugUnitTest`
  **BUILD SUCCESSFUL**, 0 failures.

